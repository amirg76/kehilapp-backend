# dependencies
/node_modules

# env files
.env
.env.*
!.env.example

# secrets and keys — nothing here exists today, and nothing should ever be able
# to stage by accident either.
secrets/
prod.env
*.pem
*.key
*.p12
*credentials*.json
id_rsa

# misc
.DS_Store
.vscode

# logs
/src/logs
# Cygwin/MSYS crash dumps — a tool crashed here once and left this behind.
*.stackdump
